{
  "config": {
    "step": {
      "user": {
        "title": "TRMNL HA Climate Connector",
        "description": "Paste the webhook URL from your TRMNL plugin's Settings page (Plugins → your plugin → Webhook URL).",
        "data": {
          "webhook_url": "TRMNL Webhook URL"
        }
      }
    },
    "error": {
      "invalid_url": "URL must start with https://",
      "cannot_connect": "Could not reach the webhook URL. Check that it is correct and that TRMNL is reachable."
    },
    "abort": {
      "already_configured": "This webhook URL is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Display & Push Settings",
        "description": "Configure charts and push interval. Requires the HA Recorder integration for line/bar charts.",
        "data": {
          "chart_count": "Number of charts",
          "chart_hours": "Chart history window",
          "push_interval": "Home Assistant push interval (minutes)",
          "chart1_sensor_type": "Chart 1 — sensor type",
          "chart1_areas": "Chart 1 — areas (empty = all)",
          "chart1_type": "Chart 1 — chart type",
          "chart2_sensor_type": "Chart 2 — sensor type",
          "chart2_areas": "Chart 2 — areas (empty = all)",
          "chart2_type": "Chart 2 — chart type"
        },
        "data_description": {
          "chart_count": "How many charts to show below the sensor values.",
          "chart_hours": "How many hours of history to show (line and bar charts only).",
          "push_interval": "Defines how frequently this integration fetches fresh sensor data and pushes it to TRMNL. Independent of the TRMNL device Refresh Rate — both need to be set.",
          "chart1_areas": "Leave empty to include all areas in Chart 1.",
          "chart2_areas": "Leave empty to include all areas in Chart 2."
        }
      }
    }
  }
}
